import { StyleSheet } from "@react-pdf/renderer";

export default StyleSheet.create({
    question: {
        fontSize: 16, fontWeight: "medium"
    },
    answer: {
        marginTop: 8, fontSize: 14, fontWeight: "normal", opacity: 0.8
    },
    viewQuestion: {
        marginTop: 30
    },
    viewQuestionMoreMargin: {
        marginTop: 50
    },
})